import { Routes } from '@angular/router';
import { UserInfoComponent } from './info/user-info/user-info/user-info.component';

export const routes: Routes = [
  { path: "home", component: UserInfoComponent }
]
